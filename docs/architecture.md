# Architecture

This repository is organized as a set of modules.

- Each `module-XX-*` folder contains a `README.md` and supporting materials.
- Labs live under the top-level `labs/` directory, grouped by module.
- Each lab folder contains `lab.ipynb` and `solution.ipynb`.
