[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-agents-mastery"
version = "0.1.0"
description = "Course scaffold for agentic AI modules, lectures, and labs."
readme = "README.md"
requires-python = ">=3.10"
